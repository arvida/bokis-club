<div class="min-h-screen flex flex-col">
  <main class="flex-1 flex items-center justify-center px-4 py-16">
    <div class="max-w-lg text-center">
      <h1 class="font-display text-6xl font-light mb-6">
        Bokis
      </h1>

      <p class="text-xl text-ink-light mb-8 leading-relaxed">
        <%= t("landing.tagline") %>
      </p>

      <p class="text-ink-muted mb-10">
        <%= t("landing.description") %>
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <%= link_to t("landing.signup"), signup_path,
            class: "px-8 py-3 bg-vermillion text-white font-medium rounded-lg hover:bg-vermillion-dark transition-colors" %>
        <%= link_to t("landing.login"), login_path,
            class: "px-8 py-3 border border-ink-muted text-ink font-medium rounded-lg hover:border-ink transition-colors" %>
      </div>
    </div>
  </main>

  <footer class="py-8 text-center text-ink-muted">
    <p class="text-sm mb-2">
      <%= t("landing.footer") %>
      <span class="mx-2">·</span>
      <%= mail_to "hi@bokis.club", t("landing.contact"), class: "hover:text-ink transition-colors" %>
    </p>
    <p class="text-xs">📲 <%= t("landing.install_hint") %></p>
  </footer>
</div>
